---
- name: Restart NetworkManager
  systemd:
    name: NetworkManager
    state: restarted

- name: Start libvirt Network
  virt_net:
    state: active
    name: "{{ network_name }}"

- name: Auto Start libvirt Network
  virt_net:
    autostart: yes
    name: "{{ network_name }}"
