#! ansible-playbook -K
---
- name: Bootstrap the base image
  hosts: localhost
  connection: local
  become: yes
  vars:
    kvm_snapshot_guest_hostnames:
    - master
    - node1
    - node2
  roles:
  - kvm_host
  - kvm_snapshot_base
  - kvm_snapshot_guest
